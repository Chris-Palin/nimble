// Phase-2 seed: the React shell (Arabella. sidebar + home) and the Stage
// port mount from here. Phase 1 intentionally ships no runtime code — the
// static site in public/ is the whole product for now.
export {};
